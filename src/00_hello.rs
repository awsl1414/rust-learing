// 00_hello.rs - Hello World 与环境配置
// 本文件展示 Rust 的基本程序结构、输出方式和环境配置检查

fn main() {
    println!("=== 🦀 欢迎来到 Rust 世界！===\n");

    // 1. 第一个 Rust 程序
    println!("1. Hello, World!");
    println!("恭喜你运行了第一个 Rust 程序！");

    // 2. 格式化输出
    println!("\n2. 格式化输出演示：");
    let name = "Rust学习者";
    let level = 1;
    println!("你好，{}！欢迎学习 Rust，当前等级：{}", name, level);

    // 3. 调试输出 (Rust 2024 Edition 改进)
    println!("\n3. 调试输出演示：");
    let x = 42;
    let y = 3.14;
    dbg!(x); // 显示变量名和值
    dbg!(y); // 更清晰的调试信息

    // 4. 特殊字符输出
    println!("\n4. 特殊字符输出：");
    println!("制表符：Hello\tWorld");
    println!("换行符：第一行\n第二行");
    println!("引号：他说：\"Rust 很棒！\"");
    println!("反斜杠：路径 C:\\\\Users\\\\");

    // 5. 环境信息检查
    println!("\n5. 环境信息：");
    print_rust_version();
    print_build_info();

    // 6. 学习路线引导
    println!("\n6. 📚 学习路线建议：");
    println!("   ✅ 完成环境配置");
    println!("   📝 下一步：学习变量与可变性 (01_variables.rs)");
    println!("   💡 提示：按顺序学习，每个概念都要理解透彻");

    println!("\n=== Hello World 学习完成 ===");
    println!("🎉 准备好开始你的 Rust 之旅了吗？");
}

// 辅助函数：显示 Rust 版本信息
fn print_rust_version() {
    // 获取当前日期（编译时）
    let date = env!("CARGO_PKG_VERSION");
    println!("   项目版本: {}", date);

    // 显示编译信息
    println!("   Rust 版本: {}", env!("CARGO_PKG_RUST_VERSION"));
}

// 辅助函数：显示构建信息
fn print_build_info() {
    println!("   目标架构: {}", std::env::consts::ARCH);
    println!("   操作系统: {}", std::env::consts::OS);
    println!("   家族: {}", std::env::consts::FAMILY);

    // 显示当前时间
    use std::time::{SystemTime, UNIX_EPOCH};
    let timestamp = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .expect("时间获取失败")
        .as_secs();
    println!("   当前时间戳: {}", timestamp);
}

/*
📖 本章重要概念总结：

1. println! 宏：
   - 用于输出文本到控制台
   - 支持格式化占位符 {}
   - 自动添加换行符

2. print! 宏：
   - 类似 println! 但不自动换行
   - 需要手动添加 \n 来换行

3. dbg! 宏：
   - 调试专用输出宏
   - 显示变量名和值
   - 自动包含文件和行号信息
   - 在 Rust 2024 Edition 中更加友好

4. 变量绑定：
   - 使用 let 关键字声明变量
   - 默认不可变（后续章节详细讲解）
   - 支持类型推断

5. 字符串：
   - 使用双引号 "..."
   - 支持 Unicode 字符
   - 可以包含转义序列

6. 注释：
   - // 单行注释
   - /* */ 多行注释
   - /// 文档注释（后续学习）

🎯 学习目标：
- [ ] 成功运行第一个 Rust 程序
- [ ] 理解 println! 和 print! 的区别
- [ ] 学会使用 dbg! 进行调试
- [ ] 了解基本的字符串输出
- [ ] 掌握注释的使用

💡 下一步提示：
运行 cargo run --bin variables 开始学习变量概念！

🔧 运行命令：
cargo run --bin hello

📚 相关资源：
- Rust 官方文档: https://doc.rust-lang.org/
- Rust by Example: https://doc.rust-lang.org/rust-by-example/
*/
